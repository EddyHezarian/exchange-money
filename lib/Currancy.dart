import 'package:flutter/material.dart';

class currancy {
  String? id;
  String? title;
  String? price;
  String? changes;
  String? status;

  currancy(
      {required this.id,
      required this.title,
      required this.price,
      required this.changes,
      required this.status});
}
